import React from 'react'
import { CollectionCreator as CollectionCreatorController } from '../../components/CollectionCreator'

export const CollectionCreator = (props) => {
  return (
    <CollectionCreatorController {...props} />
  )
}
